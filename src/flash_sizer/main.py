"""CLI entry point. Filled out as the sampler/projection/report modules land."""

from __future__ import annotations

import sys


def main() -> int:
    print(
        "valkey-flash-sizer: CLI not yet implemented. "
        "The stats layer is in place; sampling, projection, and reporting follow.",
        file=sys.stderr,
    )
    return 2
